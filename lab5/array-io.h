#include <stddef.h>

#ifndef ARRAY_IO_H__
#define ARRAY_IO_H__

/**
 * void insertion_sort(int * arr, size_t len)
 *
 * Perform an insertion sort on an array.
 *
 * @arr - pointer to array
 * @len - size of array
 */
void insertion_sort(int * arr, size_t len);

#endif